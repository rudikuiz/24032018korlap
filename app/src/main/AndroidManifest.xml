<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.piramidsoft.korlap">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Config.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_final"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_final"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginPage"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme." />
        <activity
            android:name=".SplashSCreen"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme." />
        <activity android:name=".menus.DaftarPenagihan" />
        <activity android:name=".menus.DaftarSurvey" />
        <activity android:name=".menus.DetailClientPenagihan" />
        <activity android:name=".menus.ClientNotes" />
        <activity android:name=".menus.DetailClientSurvey" />
        <activity android:name=".menus.DaftarCollector" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCbUitCVq8V5TcSW8cibEv2_lxXxfMX2gg" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".menus.DetailCollector"/>
    </application>

</manifest>